# PyQt6 Todo List（桌面置顶）

一个简洁的待办事项桌面应用，支持：

- 新增/完成/删除待办
- 自动保存到本地 `todos.json`
- 窗口置顶（可开关）

## 运行

```bash
pip install -r requirements.txt
python main.py
```

## 使用

- 在输入框输入内容后，点击「添加」或回车
- 勾选复选框标记完成
- 点击「删除」移除条目
- 勾选「窗口置顶」让窗口始终显示在最前
